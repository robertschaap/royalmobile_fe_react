const spacing = (n: number) => (n * 4) + "px";

const color = {
  primary: '#ff9e00',
  secondary: '#c3ebeb',
  grey: '#ddd',
  lightGrey: '#eee',
  black: 'black',
  white: 'white',
};

const theme = {
  color,
  spacing,
};


export default theme;
